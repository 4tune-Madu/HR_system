import React from 'react'; import {Building2,Info} from 'lucide-react'; export default function Organization(){return <div><div className="page-heading"><div><span className="eyebrow">Structure</span><h2>Organization</h2><p>Organization configuration and workforce structure.</p></div></div><div className="panel notice"><Info size={22}/><div><h3>Organization APIs are not exposed yet</h3><p>The backend currently exposes employee and authentication APIs. Branches, departments, teams, positions and job grades exist as backend models, but there are no REST endpoints for managing them yet.</p><p>The employee form therefore accepts their UUIDs until those endpoints are added.</p></div></div></div>}
