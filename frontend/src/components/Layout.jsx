import React from 'react';
import {NavLink, Outlet, useNavigate} from 'react-router-dom';
import {LayoutDashboard, Users, Building2, FileText, Settings, LogOut, Menu} from 'lucide-react';
import {useAuth} from '../context/AuthContext';
export default function Layout(){const {user,logout}=useAuth();const nav=useNavigate();const [open,setOpen]=React.useState(false); const items=[['/dashboard','Dashboard',LayoutDashboard],['/employees','Employees',Users],['/organization','Organization',Building2],['/documents','Documents',FileText],['/settings','Settings',Settings]]; return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><span>HR</span><div><strong>HR System</strong><small>People & Workforce</small></div></div><nav>{items.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'nav-item active':'nav-item'}><Icon size={19}/>{label}</NavLink>)}</nav><button className="logout" onClick={async()=>{await logout();nav('/login')}}><LogOut size={18}/>Logout</button></aside><main className="main"><header className="topbar"><button className="menu" onClick={()=>setOpen(!open)}><Menu/></button><div><h1>Human Resources</h1><p>Manage your people, employment and documents.</p></div><div className="user-chip"><div className="avatar">{user?.first_name?.[0]||user?.email?.[0]||'U'}</div><span>{user?.first_name ? `${user.first_name} ${user.last_name}` : user?.email}</span></div></header><section className="content"><Outlet/></section></main></div>}
